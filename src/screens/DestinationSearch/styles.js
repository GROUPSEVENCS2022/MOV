import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        padding: 10,
        marginTop: 35,
        /* backgroundColor: '#eee', */
        height: '100%',
    },
    textInput: {
        height: 50,
        backgroundColor: '#eee',
        marginVertical: 5,
        borderRadius: 5,
    },
});

export default styles;